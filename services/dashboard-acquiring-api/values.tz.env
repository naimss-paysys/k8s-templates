# ── Tanzania country override ─────────────────────────────────────
# Only keys that differ from values.env (base).
# Merged on top of values.env by deploy.sh --country tz
NAMESPACE=test-mmp
TAG=1.0.20-TZ
REPLICAS=2
AMBASSADOR_HOST=mixxmmp-test.tigo.co.tz
HOST_ALIAS_IP=10.245.0.169